import { useRef } from "react";
import API from "./classes/API";

const useAPI = () => {
  const createAPI = (apiURL, errorHandler, endpoints) =>
    new API(apiURL, errorHandler, endpoints);

  const api = useRef({
    createAPI,
  });
  return api.current;
};

export default useAPI;
